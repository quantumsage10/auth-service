function welcome(name: string) {
    console.log('WElcome, ' + name)
}

welcome('runi')

const user = {
    name: 'runi',
}

// dot notation error
const u1 = user.name
// const u1 = user.name
console.log(u1)
